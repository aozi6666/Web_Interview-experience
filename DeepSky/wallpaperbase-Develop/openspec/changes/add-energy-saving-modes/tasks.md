## 1. 模式模型与策略层

- [x] 1.1 扩展共享显示模式类型，新增 `ExtremeLow`，并补齐所有依赖 `WallpaperDisplayMode` 的分支与文案
- [x] 1.2 在 `SystemStatusContext` 或等效策略层引入“用户意图 + 环境约束”模式计算，默认输出 `EnergySaving`
- [x] 1.3 统一定义手动互动、手动关闭互动、全屏覆盖三者的优先级与恢复规则，避免各入口直接分叉改模式

## 2. 手动互动入口

- [x] 2.1 复用聊天框旁现有模式切换入口，实现互动按钮的开关语义：开启进入 `Interactive`，再次关闭回到 `EnergySaving`
- [x] 2.2 对不可互动壁纸保留拦截逻辑，确保不支持互动的壁纸不会错误进入 `Interactive`
- [x] 2.3 将手动互动入口接入统一策略层，确保按钮操作会更新“用户意图”而不是只改一次性运行态

## 3. 桌面五连击唤醒

- [x] 3.1 在主进程现有系统鼠标截获/转发链路中增加连续 5 次左键点击检测，优先复用 `MouseEventForwarder` 作为入口
- [x] 3.2 为五连击检测增加“命中桌面壁纸层”判定，排除任何非透明应用窗口点击，避免把普通 App 点击误判为桌面手势
- [x] 3.3 为五连击检测补齐时间窗口、重置条件和事件上报，避免误触或跨窗口串联计数
- [x] 3.4 将五连击命中事件接入统一模式策略，命中后切换到手动互动模式

## 4. 全屏分级节能

- [x] 4.1 将目标屏幕全屏检测结果从“统一静帧”扩展为分级策略：`red -> ExtremeLow`，`orange/yellow -> StaticFrame`
- [x] 4.2 让全屏分级节能覆盖手动互动，但在全屏结束后按用户意图恢复到 `Interactive` 或 `EnergySaving`
- [x] 4.3 明确 `ExtremeLow` 的底层执行行为，并补齐对应的视频/静帧/渲染控制逻辑

## 5. 验证与回归

- [x] 5.1 验证默认启动时模式为 `EnergySaving`，且无手动互动时不会错误进入 `Interactive`
- [x] 5.2 验证聊天框按钮和桌面五连击都能进入互动模式，并可手动关闭回到 `EnergySaving`
- [x] 5.3 验证全屏红色进入 `ExtremeLow`、其他覆盖进入 `StaticFrame`，且覆盖结束后能正确恢复先前用户意图
